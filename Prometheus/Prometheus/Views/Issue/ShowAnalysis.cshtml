@model Prometheus.Models.IssueViewModels

@{
    ViewBag.Title = "ShowAnalysis";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Analysis</h2>

@if (Model != null)
{
    foreach (Prometheus.Models.IssueComments item in Model.AnalysisCommentList)
    {
        <div class="row">
            <div class="panel panel-default">
                <div class="panel-heading">Analysis</div>
                <div class="panel-body">
                    @Html.Raw(item.Comment)
                </div>
                <div class="panel-footer text-right">
                    Reporter: <a href="/User/UserCenter?username=@item.Reporter">@item.Reporter.Split(new char[] { '@' })[0]</a> @item.CommentDate.ToString()
                </div>
            </div>
        </div>
    }
}

